<%= form_for(@contain) do |f| %>
  <% if @contain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contain.errors.count, "error") %> prohibited this contain from being saved:</h2>

      <ul>
      <% @contain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :assignment_id %><br />
    <%= f.number_field :assignment_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
