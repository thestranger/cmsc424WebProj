<%= form_for(@tum) do |f| %>
  <% if @tum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tum.errors.count, "error") %> prohibited this tum from being saved:</h2>

      <ul>
      <% @tum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :instructor_ssn %><br />
    <%= f.text_field :instructor_ssn %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br />
    <%= f.text_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
