<%= form_for(@submitted_answer) do |f| %>
  <% if @submitted_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@submitted_answer.errors.count, "error") %> prohibited this submitted_answer from being saved:</h2>

      <ul>
      <% @submitted_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :submission_id %><br />
    <%= f.number_field :submission_id %>
  </div>
  <div class="field">
    <%= f.label :given_answer %><br />
    <%= f.text_area :given_answer %>
  </div>
  <div class="field">
    <%= f.label :points %><br />
    <%= f.text_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
